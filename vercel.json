{
  "buildCommand": "rm -rf node_modules/.cache && rm -rf .expo && npx expo export --platform web --clear",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
